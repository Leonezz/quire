{
  "$comment": "Default judge policy. Cost-effective: a cheap screener (Claude Haiku 4.5 through Claude Code; use the full id, the `haiku` alias resolves to Sonnet) judges every case; a case the screener does not PASS, or PASSes while listing a major issue, is confirmed by Codex. Override per run with --policy / --backend / --model / --screen-model / --confirm-model.",
  "policy": "screen-then-confirm",
  "screen": {
    "backend": "claude",
    "model": "claude-haiku-4-5"
  },
  "confirm": {
    "backend": "codex",
    "model": "default"
  },
  "escalateOn": [
    "MINOR",
    "MAJOR"
  ],
  "concurrency": 3,
  "escalateOnMajorIssue": true
}
